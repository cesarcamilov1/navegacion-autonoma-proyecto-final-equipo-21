# Caché de herramientas y Python
.atl/
__pycache__/
*.pyc

# Entornos virtuales (incluye .venv-tf con TensorFlow + CUDA, ~2 GB)
.venv/
.venv-tf/
.ipynb_checkpoints/

# Material de referencia de terceros (Codevilla): 94 MB, no es del equipo
# (incluye un checkpoint pre-entrenado de CARLA de 80 MB). Se excluye del repo.
recursos-proyecto-final/

# Vista/temporales de Webots (estado por usuario)
*.wbproj
*.wbt.orig

# Modelo entrenado (se baja de Colab; se versiona solo si el equipo lo decide)
controllers/cil_drive/cil_model.keras
